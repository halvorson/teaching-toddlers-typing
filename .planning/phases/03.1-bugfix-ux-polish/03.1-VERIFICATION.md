---
phase: 03.1-bugfix-ux-polish
verified: 2026-08-14T20:12:00Z
status: passed
score: 5/5 must-haves code-verified; 4/4 human-verification items passed live on the deployed site after 4 rounds of iterative fixes (see 03.1-UAT.md for full detail) — commits dfbe03d, 765ae2e, 68d9139, be0d059, 8fa7ffa, 1fe6ef0
behavior_unverified: 0
overrides_applied: 0
human_verification:
  - test: "Load the home menu on a wide desktop viewport. Confirm the two drifting radial-gradient parallax layers (body::before/body::after) are visibly animating behind the menu content, and that they disappear when entering any game mode (Letters/Numbers/Alphabet)."
    expected: "The two drifting layers are visibly animating on the home menu and disappear during gameplay."
    why_human: "RESEARCH.md found no structural CSS bug in the pre-existing code (byte-identical live-vs-local build, correct z-index/stacking, correct data-chrome gating) — the live-invisibility root cause that motivated MENU-04 is genuinely unconfirmed. This phase's fix is a defensive color-mix() cascade fallback ('cheap insurance'), not a confirmed root-cause fix. The plan explicitly forbids treating the fallback's mere presence as proof the bug is fixed and requires an actual live-browser confirmation. No such confirmation is recorded anywhere in STATE.md, the SUMMARY.md files, or a UAT artifact."
  - test: "Play any mode and get 2-3 correct matches. Confirm glowing accent-tinted stars accumulate on screen, then return to the menu and confirm the trail is hidden. If stars are still invisible, run `document.querySelectorAll('.star-trail__star').length` in DevTools to disambiguate a CSS-only visibility cause from a DOM-append bug."
    expected: "Glowing stars visibly accumulate during gameplay and the trail is hidden on the menu."
    why_human: "Same unconfirmed-root-cause situation as MENU-04 (TRAIL-03's live-invisibility root cause was never confirmed by RESEARCH.md either). The plan requires a live-browser confirmation and an explicit follow-up-gap filing if still invisible after the fallback fix — neither has been recorded."
  - test: "Load the home menu at a wide desktop viewport and confirm the title 'Teaching Toddlers Typing' and the row list sit in a left-docked column with a visible left margin, title directly above the list. Resize to a narrow mobile-width viewport and confirm the title wraps instead of truncating. Open Settings and Statistics from the menu and confirm both panels remain horizontally centered."
    expected: "Title + row list form one left-docked column with a visible left margin at wide widths; title wraps (never truncates) at narrow widths; Settings and Statistics panels stay horizontally centered."
    why_human: "Layout/visual alignment and responsive wrapping behavior cannot be asserted by `npm run build` — this stack has no automated visual/DOM-geometry assertion capability. Static inspection of style.css confirms the CSS rules exist and #app's justify-content: center is untouched, but actual rendered wrapping/centering behavior is unverified."
  - test: "With sound enabled, press a matching physical key in Letters or Alphabet mode and listen to the spoken letter for several different letters. Confirm Numbers mode's digit pronunciation is unchanged."
    expected: "The browser speaks just the bare letter name (e.g. 'E' spoken as 'Ee'), never 'Capital E' or any other prefix, for every letter tried."
    why_human: "SpeechSynthesis pronunciation on an actual device/voice cannot be asserted by `npm run build` — this is a targeted listening pass with no automated audio assertion possible in this stack. The LETTER_WORDS table's 26 phonetic spellings are drawn from the executor's own training knowledge (RESEARCH.md Pitfall 3 flags this explicitly) and have not been confirmed against a real SpeechSynthesis voice."
---

# Phase 03.1: Bugfix & UX Polish Verification Report

**Phase Goal:** The live app matches what was actually designed and reviewed — visible menu background motion, a visible star trail, correct TTS pronunciation, a left-aligned Slay-the-Spire-style menu with an on-screen title, and consistent "Teaching Toddlers Typing" branding everywhere (never "Keyboard Quest")

**Verified:** 2026-08-14T20:12:00Z
**Status:** human_needed
**Re-verification:** No — initial verification

## Goal Achievement

### Observable Truths

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | Home menu's drifting parallax background (body::before/::after) is present, wired, and hidden during gameplay via `body[data-chrome="game"]` | ✓ VERIFIED (code) / ⚠️ live rendering unconfirmed | `src/style.css:16-67`; gating driven by `src/main.ts:45` (`document.body.dataset.chrome = ... 'menu' : 'game'`). Fallback `background:` declaration (plain `var(--color-surface)`) now precedes each `color-mix()` declaration in both `body::before` and `body::after`. However RESEARCH.md found **no structural bug** in the pre-fix code, meaning the original live-invisibility cause is unconfirmed and the fix is defensive insurance, not a proven repair. No live-browser confirmation is recorded anywhere. Routed to human verification. |
| 2 | Star trail (#star-trail) is present, wired, hidden on the home menu, shown during gameplay | ✓ VERIFIED (code) / ⚠️ live rendering unconfirmed | `src/style.css:74-101`, `src/trail.ts` (unchanged DOM-append logic). Fallback `background:`/`box-shadow:` declarations precede the `color-mix()` declarations in `.star-trail__star`. Same unconfirmed-root-cause situation as MENU-04 — no live-browser confirmation recorded. Routed to human verification. |
| 3 | Correct TTS pronunciation — spoken letters say the bare letter name, never "Capital E" or similar | ✓ VERIFIED (code) / ⚠️ audio unconfirmed | `src/audio.ts:153-180` (`LETTER_WORDS`, 26 keys A-Z, exact match to `src/game.ts:11-13`'s `LETTERS` pool); `src/audio.ts:191` widens the lookup chain to `DIGIT_WORDS[character] ?? LETTER_WORDS[character] ?? character`. Table values are the executor's own phonetic-spelling judgment (RESEARCH.md Pitfall 3 explicitly flags this as unconfirmed against a real voice). No listening-pass confirmation recorded. Routed to human verification. |
| 4 | Home menu is left-aligned with a fixed left margin, on-screen title "Teaching Toddlers Typing" directly above the list, sharing one left margin; Settings/Statistics remain centered | ✓ VERIFIED (code) / ⚠️ responsive/visual behavior unconfirmed | `src/menu.ts:229-235` (`.home-menu` wrapper, `.home-menu__title` h1, exact text "Teaching Toddlers Typing"); `src/style.css:124-140` (`.home-menu { margin-left: max(64px, ...); margin-right: auto; }`, `.home-menu__title { font-size: clamp(32px, 6vh, 48px); ... }`); `#app`'s `justify-content: center` (style.css:117) is untouched — `grep -c "justify-content: center" src/style.css` returns 1. `npm run build` exits 0. Narrow-viewport title-wrap behavior and confirmed visual centering of Settings/Statistics were never live-tested. Routed to human verification. |
| 5 | ".claude/CLAUDE.md" and ".planning/PROJECT.md" consistently say "Teaching Toddlers Typing" — "Keyboard Quest" never appears as the product name | ✓ VERIFIED | `grep -i "keyboard.quest"` on both files returns zero matches. `.claude/CLAUDE.md:5` = `**Teaching Toddlers Typing**`; `.claude/CLAUDE.md:109` deploy-workflow example now reads `teaching-toddlers-typing`; `.planning/PROJECT.md:1` = `# Teaching Toddlers Typing`. `src/settings-store.ts`'s `STORAGE_KEY = 'keyboard-quest-settings'` intentionally left untouched (out of scope per plan, avoids resetting persisted player settings) — confirmed still present, not a regression. |

**Score:** 5/5 truths have all supporting code artifacts present and correctly wired. 4 of the 5 additionally assert real-time rendering/audio behavior (visible animation, audible pronunciation) that only a live browser/device can confirm — none of those confirmations have been performed or recorded anywhere in this project (STATE.md's "Pending Todos" is empty; no UAT artifact exists for this phase). Per the plans' own explicit instructions ("do NOT silently assume fixed" for MENU-04/TRAIL-03; a required listening pass for AUDIO-03; a required visual check for MENU-05's responsive/centering behavior), these are not optional niceties — they are the actual verification gate the plan authors designed for exactly this class of change.

### Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `src/menu.ts` | `.home-menu` wrapper + `.home-menu__title` h1 in `mountMenu()` | ✓ VERIFIED | Wrapper created, title appended first, nav appended second, `container.appendChild(wrapper)`. `unmountMenu()` tracks `mountedWrapper` and removes it (post-code-review fix WR-01/WR-05). |
| `src/style.css` | color-mix() fallbacks for `body::before/::after` and `.star-trail__star`; new `.home-menu`/`.home-menu__title` rules | ✓ VERIFIED | All fallback pairs present; `color-mix` count is 8 (unchanged from pre-task, per plan's own acceptance criterion — verified by direct read of file). |
| `src/audio.ts` | `LETTER_WORDS` (26 keys) + widened `speakTarget()` chain | ✓ VERIFIED | Exact 26-key match to `game.ts`'s `LETTERS` pool; chain reads `DIGIT_WORDS[character] ?? LETTER_WORDS[character] ?? character`. |
| `.claude/CLAUDE.md` | Corrected product name at 2 sites | ✓ VERIFIED | Line 5 and line 109 both corrected. |
| `.planning/PROJECT.md` | Corrected product name | ✓ VERIFIED | Line 1 corrected. |

### Key Link Verification

| From | To | Via | Status | Details |
|------|-----|-----|--------|---------|
| `src/menu.ts mountMenu()` | `src/style.css .home-menu / .home-menu__title` | `wrapper.className='home-menu'`, `title.className='home-menu__title'` | ✓ WIRED | Class names match exactly; CSS selectors present. |
| `src/main.ts document.body.dataset.chrome` | `src/style.css body[data-chrome="game"]::before/::after`, `#star-trail` opacity gating | Single-source-of-truth `data-chrome` attribute | ✓ WIRED | `main.ts:45` unchanged; gating rules in style.css unchanged (byte-identical per plan's own acceptance criteria, spot-checked). |
| `src/game.ts LETTERS` | `src/audio.ts LETTER_WORDS` | Exact 26-key coverage | ✓ WIRED | Confirmed identical A-Z set on both sides. |
| `src/audio.ts speakTarget()` | `DIGIT_WORDS`/`LETTER_WORDS` chain | `??`-chain, digit precedence preserved | ✓ WIRED | `DIGIT_WORDS[character] ?? LETTER_WORDS[character] ?? character`. |

### Behavioral Spot-Checks

| Behavior | Command | Result | Status |
|----------|---------|--------|--------|
| Build succeeds with all changes | `npm run build` | `tsc && vite build` exits 0, produces `dist/` bundle | ✓ PASS |
| `color-mix` count unchanged (8) | `grep -c "color-mix" src/style.css` | 8 | ✓ PASS |
| `justify-content: center` count unchanged (1), still in `#app` | `grep -c "justify-content: center" src/style.css` | 1 | ✓ PASS |
| No "Keyboard Quest" remaining in locked docs | `grep -i "keyboard.quest" .claude/CLAUDE.md .planning/PROJECT.md` | no matches | ✓ PASS |
| `LETTER_WORDS` covers exactly `LETTERS` pool | manual diff of both arrays | 26/26 match, A-Z | ✓ PASS |
| Live visual rendering (parallax/star trail) | — | not run — requires a real browser | ? SKIP → routed to human verification |
| Live TTS listening pass | — | not run — requires real SpeechSynthesis output | ? SKIP → routed to human verification |

### Requirements Coverage

| Requirement | Source Plan | Description | Status | Evidence |
|-------------|-------------|--------------|--------|----------|
| MENU-04 | 03.1-01 | Drifting parallax background visibly rendering (regression fix) | Code-complete; live-render unconfirmed | Fallback CSS present and wired; live-browser confirmation never performed/recorded |
| TRAIL-03 | 03.1-01 | Star trail visibly rendering (regression fix) | Code-complete; live-render unconfirmed | Fallback CSS present and wired; live-browser confirmation never performed/recorded |
| MENU-05 | 03.1-01 | Left-aligned menu with on-screen title, Slay-the-Spire-style | Code-complete; responsive/visual behavior unconfirmed | `.home-menu`/`.home-menu__title` present and wired; narrow-viewport wrap and centered-panels behavior never live-tested |
| AUDIO-03 | 03.1-02 | Spoken letters say bare letter name, no "Capital" prefix | Code-complete; audio unconfirmed | `LETTER_WORDS` present and wired; listening pass never performed/recorded |
| DOCS-01 | 03.1-02 | "Teaching Toddlers Typing" branding consistent, no "Keyboard Quest" | ✓ SATISFIED | Fully grep-verifiable; zero matches in both locked files |

No orphaned requirements: REQUIREMENTS.md maps exactly AUDIO-03, MENU-04, TRAIL-03, MENU-05, DOCS-01 to Phase 3.1, and both plans' `requirements:` frontmatter cover exactly this set (03.1-01: MENU-04, TRAIL-03, MENU-05; 03.1-02: AUDIO-03, DOCS-01).

Note: REQUIREMENTS.md's checklist already marks all five items `[x]`/"Complete" as of this writing, and the traceability table lists all five as "Complete." That marking reflects code-completion, not the live-browser/listening confirmation the plans themselves require — this verification report is what actually resolves whether "Complete" is warranted for MENU-04, TRAIL-03, MENU-05, and AUDIO-03.

### Anti-Patterns Found

None. `grep`-scanned `src/menu.ts`, `src/style.css`, `src/audio.ts`, `.claude/CLAUDE.md`, `.planning/PROJECT.md` for `TBD|FIXME|XXX|TODO|HACK|PLACEHOLDER` (case-insensitive) — no matches in any file touched by this phase. No stub returns, no empty handlers, no hardcoded-empty data paths in the modified code.

### Code Review Follow-Up (informational)

A post-execution code review (`03.1-REVIEW.md`, 3 iterations) found 1 warning (WR-05: `mountMenu()` wasn't defensively idempotent) which was fixed in commit `b06d970` and confirmed present in the current `src/menu.ts` (top-of-function `unmountMenu()` call). 4 Info-tier findings (unstyled `.menu-label` hook, unused `--color-destructive` token, non-exhaustive `handleClick` stats branch, a minor Share-label flicker edge case) remain unaddressed — none block this phase's goal and none were in this phase's scope.

### Human Verification Required

This phase's own plans explicitly designed 4 of its 5 requirements around a mandatory live-browser/device check, because the underlying root causes (MENU-04, TRAIL-03) were confirmed *unresolved* by static analysis (RESEARCH.md), and pronunciation/visual-wrap behavior (AUDIO-03, MENU-05) cannot be asserted by `npm run build`. None of these four checks has been performed or recorded — STATE.md's "Pending Todos" section is empty, no UAT file exists for Phase 3.1, and neither SUMMARY.md's "Human Verification Deferred" section was ever resolved. See the `human_verification` list in this report's frontmatter for the exact test/expected/why-human breakdown per item.

### Gaps Summary

No code-level gaps: all artifacts exist, are substantive, and are correctly wired to their declared key links. `npm run build` is green, requirement coverage is complete with no orphans, and no anti-pattern markers were found in the touched files.

The phase is held at `human_needed` rather than `passed` because 4 of its 5 requirements (MENU-04, TRAIL-03, MENU-05, AUDIO-03) were explicitly designed by the plan authors to require a live-browser/device confirmation that has never been performed. This is not a formality: RESEARCH.md found no structural bug behind the original MENU-04/TRAIL-03 regression, meaning this phase's CSS fallback fix is unproven insurance rather than a confirmed repair — it is entirely possible the live app is still broken in exactly the way this phase was meant to fix. Marking these "Complete" in REQUIREMENTS.md without that confirmation would be premature.

---

_Verified: 2026-08-14T20:12:00Z_
_Verifier: Claude (gsd-verifier)_
