---
phase: 03.1-bugfix-ux-polish
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/audio.ts
  - .claude/CLAUDE.md
  - .planning/PROJECT.md
autonomous: true
requirements: [AUDIO-03, DOCS-01]

estimate:
  tokens: 22000
  raw_tokens: 22000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "Spoken letters (Letters/Alphabet modes) say just the bare letter name (e.g. 'Ee' for E), never 'Capital E' or any other prefix"
    - "Numbers mode's digit pronunciation is unchanged — still speaks the English digit word via the untouched DIGIT_WORDS path"
    - ".claude/CLAUDE.md and .planning/PROJECT.md consistently refer to the project as 'Teaching Toddlers Typing' — 'Keyboard Quest' does not appear as the product name anywhere in either file, in any casing"
    - "speakTarget() is never called with an empty, null, or out-of-pool character — its only call site (game-screen.ts, unchanged this phase) passes the just-matched target character from LETTERS or DIGITS (game.ts)"
    - "LETTER_WORDS keys are exact-match single uppercase ASCII characters (A-Z); lookup is a plain object property access with no case-folding, Unicode normalization, or multi-code-point key ever required"
    - "grep -i 'keyboard.quest' across .claude/CLAUDE.md and .planning/PROJECT.md returns zero matches after this task — including both the 'Keyboard Quest' title-case header occurrences and the 'keyboard-quest' kebab-case URL-slug example"
    - "DOCS-01's replacement is exact literal (case-insensitive verified) substring matching — 'Keyboard Quest' becomes 'Teaching Toddlers Typing' and 'keyboard-quest' becomes 'teaching-toddlers-typing', two distinct casing variants, both corrected to their respective correct-case forms"
  artifacts:
    - path: "src/audio.ts"
      provides: "LETTER_WORDS lookup table (26 keys, A-Z) and updated speakTarget() consumption chain"
      contains: "LETTER_WORDS"
    - path: ".claude/CLAUDE.md"
      provides: "Corrected product name at both sites (header line 5, deploy-workflow example line 109)"
    - path: ".planning/PROJECT.md"
      provides: "Corrected product name (header line 1)"
  key_links:
    - from: "src/game.ts LETTERS pool"
      to: "src/audio.ts LETTER_WORDS keys"
      integration: "LETTER_WORDS must cover exactly the 26 characters in LETTERS — no extra key, no missing key"
    - from: "src/audio.ts speakTarget()"
      to: "DIGIT_WORDS / LETTER_WORDS lookup chain"
      integration: "DIGIT_WORDS[character] ?? LETTER_WORDS[character] ?? character — DIGIT_WORDS precedence preserved"
  prohibitions:
    - requirement_id: AUDIO-03
      category: values
      status: resolved
      verification: judgment
      resolution: null
      reason: null
      statement: "MUST NOT introduce a mispronunciation in LETTER_WORDS that teaches the child an incorrect letter-sound association — the app's stated Core Value is that the correct-match feedback loop teaches the letter/key association, so a wrong spelled-word entry directly undermines the product's purpose"
    - requirement_id: DOCS-01
      category: safety
      status: resolved
      verification: judgment
      resolution: null
      reason: null
      statement: "MUST NOT expand DOCS-01's find-replace beyond the two locked files into src/settings-store.ts's STORAGE_KEY ('keyboard-quest-settings') or other frozen historical docs — a bare key rename with no migration path would silently reset every existing player's persisted sound/trail settings"
---

## Phase Goal (MVP framing note)

ROADMAP.md's Phase 3.1 `**Goal:**` line is not in "As a / I want to / so that" format — it is
a bugfix/regression-restoration goal, not a new-capability user story. Per the user-story
template's "do not invent a story" rule, no story is fabricated here. Goal, verbatim from
ROADMAP.md:

> The live app matches what was actually designed and reviewed — visible menu background
> motion, a visible star trail, correct TTS pronunciation, a left-aligned Slay-the-Spire-style
> menu with an on-screen title, and consistent "Teaching Toddlers Typing" branding everywhere
> (never "Keyboard Quest")

This plan covers the non-visual half of that goal: AUDIO-03 (TTS pronunciation) and DOCS-01
(branding correction).

## Artifacts This Phase Produces (this plan's share)

- `src/audio.ts`: new module-private `LETTER_WORDS: Readonly<Record<string, string>>` constant
  (26 keys, A-Z); `speakTarget()`'s `spoken` assignment widened to
  `DIGIT_WORDS[character] ?? LETTER_WORDS[character] ?? character`
- `.claude/CLAUDE.md`: corrected product-name text (2 sites)
- `.planning/PROJECT.md`: corrected product-name text (1 site)

<objective>
Fix the TTS "Capital E" mispronunciation via an explicit letter-name lookup table mirroring the
existing DIGIT_WORDS pattern, and correct the stale "Keyboard Quest" product name to
"Teaching Toddlers Typing" in the two locked planning/config docs.

Purpose: AUDIO-03 restores the celebratory-speech feature to its intended pedagogical behavior
(teaching the correct letter name, not a garbled prefix); DOCS-01 removes the last stale-branding
artifacts from tracked project docs.
Output: `src/audio.ts` gains LETTER_WORDS + a widened speakTarget() lookup chain; `.claude/
CLAUDE.md` and `.planning/PROJECT.md` say "Teaching Toddlers Typing" everywhere.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03.1-bugfix-ux-polish/03.1-CONTEXT.md
@.planning/phases/03.1-bugfix-ux-polish/03.1-RESEARCH.md
@.planning/phases/03.1-bugfix-ux-polish/03.1-PATTERNS.md
@src/audio.ts
@.claude/CLAUDE.md
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: AUDIO-03 — LETTER_WORDS pronunciation fix, wired end-to-end</name>
  <files>src/audio.ts</files>
  <read_first>
    src/audio.ts (full file, especially DIGIT_WORDS lines 124-143 and speakTarget() lines
    151-179), src/game.ts lines 11-14 (LETTERS pool — the exact 26-key contract LETTER_WORDS
    must satisfy), 03.1-RESEARCH.md "Code Examples — AUDIO-03: LETTER_WORDS lookup table"
    section, 03.1-RESEARCH.md "Pitfall 3" (spelled-word values are training-knowledge, flag for
    a listening pass)
  </read_first>
  <action>
    In src/audio.ts, add a new `const LETTER_WORDS: Readonly&lt;Record&lt;string, string&gt;&gt; = { ... }`
    immediately after the existing DIGIT_WORDS constant (after line 143), covering exactly the
    26 keys in game.ts's LETTERS array (A through Z), each mapped to its spelled-out English
    letter name: A: 'Ay', B: 'Bee', C: 'See', D: 'Dee', E: 'Ee', F: 'Eff', G: 'Jee', H: 'Aitch',
    I: 'Eye', J: 'Jay', K: 'Kay', L: 'El', M: 'Em', N: 'En', O: 'Oh', P: 'Pee', Q: 'Cue', R: 'Ar',
    S: 'Ess', T: 'Tee', U: 'You', V: 'Vee', W: 'Double-you', X: 'Ex', Y: 'Why', Z: 'Zee'.
    Add a doc comment above LETTER_WORDS mirroring DIGIT_WORDS' comment shape but WITHOUT
    repeating the false "English voices read a bare capital letter as its letter name"
    assumption — state instead that a bare uppercase letter is not a pronunciation the Speech
    API guarantees across voices/OS combinations (some prepend "Capital"), so this table removes
    the ambiguity the same way DIGIT_WORDS does for digits.
    Edit DIGIT_WORDS' own doc comment to remove its now-false final sentence ("Uppercase A-Z
    needs no mapping: English voices read a bare capital letter as its letter name.") since
    LETTER_WORDS exists specifically because that assumption is false.
    Change line 154's `const spoken = DIGIT_WORDS[character] ?? character` to
    `const spoken = DIGIT_WORDS[character] ?? LETTER_WORDS[character] ?? character`, preserving
    the existing ??-chain idiom and DIGIT_WORDS' precedence.
    No new imports, no changes to playTone, playChime, primeVoices, stopSpeech, or the
    utterance.lang/voice-selection logic (lines 166-173) — the utterance.lang fallback quirk
    (RESEARCH.md Pitfall 4) is explicitly out of this task's scope.
  </action>
  <acceptance_criteria>
    - src/audio.ts defines LETTER_WORDS as a Readonly&lt;Record&lt;string, string&gt;&gt; with exactly 26
      keys, one per letter in game.ts's LETTERS array (A-Z), no extras and no omissions.
    - speakTarget()'s spoken assignment reads
      `DIGIT_WORDS[character] ?? LETTER_WORDS[character] ?? character`.
    - The DIGIT_WORDS doc comment no longer contains the sentence "Uppercase A-Z needs no
      mapping: English voices read a bare capital letter as its letter name."
    - npm run build exits 0 with no TypeScript errors (a shape mismatch between DIGIT_WORDS and
      LETTER_WORDS would surface as a type error since both are Readonly&lt;Record&lt;string,string&gt;&gt;).
  </acceptance_criteria>
  <verify>
    <automated>npm run build</automated>
    <human-check>With sound enabled, press a matching physical key in Letters or Alphabet mode
    and listen: the browser must speak just the bare letter name (e.g. "E" spoken as "Ee"),
    never "Capital E" or any other prefix. Confirm Numbers mode's digit pronunciation is
    unchanged. This is a targeted listening pass on the actual device/voice per RESEARCH.md
    Pitfall 3 — if any single letter sounds wrong, fix only that table entry, not the lookup
    mechanism.</human-check>
  </verify>
  <reversibility rating="reversible">A single new lookup table plus a one-line consumption-chain
  widening; trivially revertible via git, no persisted-data implications.</reversibility>
  <done>LETTER_WORDS added, speakTarget() consumes it via the existing ??-chain, DIGIT_WORDS'
  false comment removed, build green.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: DOCS-01 — branding correction in the two locked doc files</name>
  <files>.claude/CLAUDE.md, .planning/PROJECT.md</files>
  <read_first>
    .claude/CLAUDE.md (full file, especially lines 1-10 and 100-119), .planning/PROJECT.md
    (lines 1-10), 03.1-RESEARCH.md "Runtime State Inventory" and "Code Examples — DOCS-01"
    sections, 03.1-PATTERNS.md "CLAUDE.md / PROJECT.md — DOCS-01" section
  </read_first>
  <action>
    In .claude/CLAUDE.md line 5, replace `**Keyboard Quest**` with
    `**Teaching Toddlers Typing**`.
    In .claude/CLAUDE.md line 109 (GitHub Actions Deployment Workflow bullet), replace the
    example URL/base-path pair `https://&lt;username&gt;.github.io/keyboard-quest/` ... `base:
    '/keyboard-quest/'` with `https://&lt;username&gt;.github.io/teaching-toddlers-typing/` ...
    `base: '/teaching-toddlers-typing/'` — this line was missed by RESEARCH.md's grep pass and
    its slug matches the project's actual repo/deploy name (`teaching-toddlers-typing`,
    confirmed in package.json's "name" field).
    In .planning/PROJECT.md line 1, replace `# Keyboard Quest` with
    `# Teaching Toddlers Typing`.
    Do NOT touch src/settings-store.ts's `STORAGE_KEY = 'keyboard-quest-settings'` — explicitly
    out of scope (renaming would reset every existing player's persisted settings with no
    migration path, per RESEARCH.md's Runtime State Inventory and Open Question 1).
    Do NOT touch historical/frozen docs (.planning/research/*.md, prior phase PLAN.md/
    SUMMARY.md files) — only the two locked files above.
  </action>
  <acceptance_criteria>
    - grep -ic "keyboard.quest" .claude/CLAUDE.md returns 0.
    - grep -ic "keyboard.quest" .planning/PROJECT.md returns 0.
    - grep -n "Teaching Toddlers Typing" .claude/CLAUDE.md matches at least the corrected line 5.
    - grep -n "teaching-toddlers-typing" .claude/CLAUDE.md matches the corrected deploy-workflow
      example line.
    - grep -c "keyboard-quest" src/settings-store.ts still returns 1 (STORAGE_KEY intentionally
      untouched).
    - npm run build exits 0 (docs are not part of the build; confirms no accidental edit to
      source files).
  </acceptance_criteria>
  <verify>
    <automated>grep -ic "keyboard.quest" .claude/CLAUDE.md .planning/PROJECT.md | grep -v ':0' | wc -l | grep -qx 0 &amp;&amp; npm run build</automated>
    <human-check>Read both files top to bottom and confirm no remaining "Keyboard Quest" text in
    any casing, and that the corrected text reads naturally in context (not just a raw
    substring swap that breaks a sentence).</human-check>
  </verify>
  <reversibility rating="reversible">Two-file text edit with no code/runtime impact; trivially
  revertible via git.</reversibility>
  <done>No case variant of "Keyboard Quest"/"keyboard-quest" remains in either locked doc file;
  STORAGE_KEY unchanged; every other tracked doc was already correct.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| None crossed | Both tasks are static edits (a compile-time lookup table, two doc-text replacements) with no new input surface, no network call, no persisted-data change. Per RESEARCH.md's Security Domain analysis: V5 Input Validation is marginal at most — speakTarget()'s input is always a pre-validated pool member from LETTERS/DIGITS (game.ts), never raw user input. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03.1-03 | Tampering | src/audio.ts LETTER_WORDS static lookup | low | accept | Static, developer-authored table; speakTarget()'s only input (character) is always a pre-validated pool member from LETTERS/DIGITS (game.ts), never raw user input — matches RESEARCH.md's Security Domain V5 analysis. |
| T-03.1-04 | Tampering | .claude/CLAUDE.md / .planning/PROJECT.md text edit | low | accept | Documentation-only edit, not runtime code; no execution path reads these files, so no attack surface exists. |
</threat_model>

<verification>
`npm run build` (runs `tsc` and `vite build`) after every task — this is the only automated check
available in this project (no test framework, confirmed in 03.1-VALIDATION.md). AUDIO-03
additionally requires a human listening pass (no automated audio assertion is feasible for
SpeechSynthesis output); DOCS-01 is fully automatable via case-insensitive grep.
</verification>

<success_criteria>
- `npm run build` exits 0 after both tasks.
- `grep -ic "keyboard.quest" .claude/CLAUDE.md .planning/PROJECT.md` both return 0.
- A correct key match with sound enabled speaks the bare letter name, confirmed via human-check.
</success_criteria>

<output>
Create `.planning/phases/03.1-bugfix-ux-polish/03.1-02-SUMMARY.md` when done
</output>
