---
phase: 03.1
slug: bugfix-ux-polish
# status lifecycle: draft (seeded by plan-phase) → validated (set by validate-phase §6)
# audit-milestone §5.5 distinguishes NOT-VALIDATED (draft) from PARTIAL (validated + nyquist_compliant: false) (#2117)
status: draft
nyquist_compliant: false
wave_0_complete: false
created: 2026-08-14
---

# Phase 03.1 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution.

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | None — confirmed no test files (`*.test.*`, `*.spec.*`) and no `vitest.config.*`/`jest.config.*` anywhere in the repo. Matches `.claude/CLAUDE.md`'s explicit "What NOT to Use" guidance excluding Jest/Vitest/Playwright for this hobby-scope project. |
| **Config file** | none |
| **Quick run command** | `npm run build` (runs `tsc && vite build` — the only automated check available: type-correctness + successful bundling) |
| **Full suite command** | `npm run build` (same — no separate suite) |
| **Estimated runtime** | ~5 seconds |

---

## Sampling Rate

- **After every task commit:** Run `npm run build`
- **After every plan wave:** Run `npm run build` + a manual pass through the visual/audio requirements in a real browser (dev server or the deployed preview) — this project has no automated visual/audio assertion capability
- **Before `/gsd:verify-work`:** `npm run build` must be green AND all five requirements manually walked through in a real browser/device — none of MENU-04/TRAIL-03/MENU-05/AUDIO-03 has an automated assertion possible in this stack
- **Max feedback latency:** 5 seconds (build) / immediate (manual visual/audio check per task)

---

## Per-Task Verification Map

| Task ID | Plan | Wave | Requirement | Threat Ref | Secure Behavior | Test Type | Automated Command | File Exists | Status |
|---------|------|------|-------------|------------|-----------------|-----------|-------------------|-------------|--------|
| 03.1-01-TBD | 01 | 1 | AUDIO-03 | — | N/A — no new input surface | manual (audio) | `npm run build` (typecheck only; pronunciation must be listened to) | N/A | ⬜ pending |
| 03.1-01-TBD | 01 | 1 | MENU-04 | — | N/A | manual (visual) | `npm run build` (CSS syntax only; visibility must be eyeballed) | N/A | ⬜ pending |
| 03.1-01-TBD | 01 | 1 | TRAIL-03 | — | N/A | manual (visual) | `npm run build` (CSS syntax only; visibility must be eyeballed) | N/A | ⬜ pending |
| 03.1-01-TBD | 01 | 1 | MENU-05 | — | N/A | manual (visual) | `npm run build` (CSS syntax only; layout must be eyeballed) | N/A | ⬜ pending |
| 03.1-01-TBD | 01 | 1 | DOCS-01 | — | N/A | automated | `grep -L "Keyboard Quest" .claude/CLAUDE.md .planning/PROJECT.md` — empty output is a pass | ✓ | ⬜ pending |

*Status: ⬜ pending · ✅ green · ❌ red · ⚠️ flaky*

*Exact Task IDs to be filled in by the planner once PLAN.md task numbering is assigned; this map records the requirement → verification-method contract the planner must honor.*

---

## Wave 0 Requirements

*None — this project deliberately has no test framework (see Test Infrastructure above). The gap is manual QA capacity, not missing test infrastructure: the plan needs an explicit human-verification task/checkpoint for each of AUDIO-03, MENU-04, TRAIL-03, MENU-05, since research had no tool available to visually or aurally confirm any of them in a real browser.*

---

## Manual-Only Verifications

| Behavior | Requirement | Why Manual | Test Instructions |
|----------|-------------|------------|-------------------|
| Spoken letter has no "Capital" prefix | AUDIO-03 | SpeechSynthesis output can only be confirmed by listening on a real device/voice; no automated audio assertion is feasible | Play Letters/Alphabet mode, press a matching key, listen for the bare letter name (e.g. "E"), never "Capital E" |
| Drifting background visibly animates on menu, hidden on game screens | MENU-04 | Visual regression — no browser automation tool was available during research; requires human eyes on a real browser | Load the home menu, confirm the two radial-gradient layers visibly drift; enter a game mode, confirm they disappear |
| Star appears at each correct match, hidden on menu | TRAIL-03 | Visual regression — same tooling gap as above | Play any mode, get 2-3 correct matches, confirm glowing stars accumulate on the background; return to menu, confirm the trail is hidden |
| Home menu is left-docked with title above it, Settings/Stats unaffected | MENU-05 | New layout — visual confirmation of alignment/spacing needs a real viewport, not just build success | Load the home menu, confirm left margin + title above the list; open Settings/Stats, confirm they remain centered |

---

## Validation Sign-Off

- [ ] All tasks have `<automated>` verify or Wave 0 dependencies (N/A — no test framework; manual verification is the documented alternative per above)
- [ ] Sampling continuity: no 3 consecutive tasks without automated verify (build-only automation applies to every task; manual visual/audio checks supplement per requirement)
- [ ] Wave 0 covers all MISSING references (none — no Wave 0 needed, see above)
- [ ] No watch-mode flags
- [ ] Feedback latency < 5s (build) — manual checks are immediate/on-demand
- [ ] `nyquist_compliant: true` set in frontmatter (deferred until `/gsd:validate-phase` confirms — this project's lack of a test framework means most requirements route through documented Manual-Only Verifications rather than automated coverage)

**Approval:** pending
