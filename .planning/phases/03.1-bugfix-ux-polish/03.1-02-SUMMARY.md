---
phase: 03.1-bugfix-ux-polish
plan: 02
subsystem: audio, docs
tags: [speech-synthesis, tts, branding, docs]

# Dependency graph
requires:
  - phase: 03-audio-modes
    provides: speakTarget() and the existing DIGIT_WORDS lookup-table pattern in src/audio.ts
provides:
  - LETTER_WORDS lookup table (26 keys, A-Z) restoring correct spoken letter names
  - Corrected "Teaching Toddlers Typing" product name in .claude/CLAUDE.md and .planning/PROJECT.md
affects: [04-statistics, 04.1-feature-enhancements]

# Actuals (#2632)
actuals:
  tokens: 886
  tasks: 2
  commits: 2

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Character-name lookup tables (DIGIT_WORDS, LETTER_WORDS) combined via a ??-chain in speakTarget(), each table owned by audio.ts (pronunciation) while game.ts owns the character pools"

key-files:
  created: []
  modified:
    - src/audio.ts
    - .claude/CLAUDE.md
    - .planning/PROJECT.md

key-decisions:
  - "LETTER_WORDS values are spelled-out phonetic English letter names (Ay, Bee, See, ... Zee), matching DIGIT_WORDS' existing convention of a plain lowercase-word target string for SpeechSynthesisUtterance"
  - "DIGIT_WORDS' doc comment corrected to remove the now-disproven claim that bare capital letters need no pronunciation mapping"
  - "src/settings-store.ts's STORAGE_KEY ('keyboard-quest-settings') intentionally left untouched — renaming would silently reset every existing player's persisted settings with no migration path"

patterns-established:
  - "Pronunciation lookup tables live in audio.ts, keyed exactly to game.ts's character pools, combined via nullish-coalescing chain with DIGIT_WORDS taking precedence over LETTER_WORDS"

requirements-completed: [AUDIO-03, DOCS-01]

coverage:
  - id: D1
    description: "LETTER_WORDS lookup table added to src/audio.ts (26 keys, A-Z) and wired into speakTarget()'s pronunciation chain, fixing the 'Capital E'-style mispronunciation"
    requirement: AUDIO-03
    verification:
      - kind: other
        ref: "npm run build (tsc + vite build) — exit 0"
        status: pass
    human_judgment: true
    rationale: "SpeechSynthesis pronunciation on the actual device/voice cannot be asserted by a build check — requires a human listening pass per the plan's <human-check>."
  - id: D2
    description: "'Keyboard Quest' branding corrected to 'Teaching Toddlers Typing' in .claude/CLAUDE.md (header + deploy-workflow example) and .planning/PROJECT.md (header)"
    requirement: DOCS-01
    verification:
      - kind: other
        ref: "grep -ic 'keyboard.quest' .claude/CLAUDE.md .planning/PROJECT.md — both return 0"
        status: pass
      - kind: other
        ref: "npm run build — exit 0 (confirms no accidental source edit)"
        status: pass
    human_judgment: false

# Metrics
duration: 5min
completed: 2026-08-14
status: complete
---

# Phase 03.1 Plan 02: TTS Letter Pronunciation & Branding Correction Summary

**Added LETTER_WORDS lookup table fixing "Capital E"-style TTS mispronunciation, and corrected stale "Keyboard Quest" branding to "Teaching Toddlers Typing" in the two locked doc files**

## Performance

- **Duration:** 5 min
- **Started:** 2026-08-14T19:42:40Z
- **Completed:** 2026-08-14T19:44:19Z
- **Tasks:** 2
- **Files modified:** 3

## Accomplishments
- `src/audio.ts` gained a `LETTER_WORDS: Readonly<Record<string, string>>` constant (26 keys, A-Z) spelling out each letter's phonetic name, mirroring the existing `DIGIT_WORDS` pattern
- `speakTarget()`'s pronunciation chain widened to `DIGIT_WORDS[character] ?? LETTER_WORDS[character] ?? character`, preserving digit precedence and the existing ??-chain idiom
- Removed `DIGIT_WORDS`' stale doc-comment claim that bare capital letters need no mapping (the entire reason `LETTER_WORDS` now exists)
- "Keyboard Quest" replaced with "Teaching Toddlers Typing" at all three sites across the two locked docs: `.claude/CLAUDE.md` header (line 5), its deploy-workflow URL/base-path example (line 109, both `github.io/keyboard-quest/` and `base: '/keyboard-quest/'` → `teaching-toddlers-typing`), and `.planning/PROJECT.md` header (line 1)
- `src/settings-store.ts`'s `STORAGE_KEY` left untouched by design (verified still 2 occurrences of `keyboard-quest`) — a rename would silently reset every existing player's persisted sound/trail settings with no migration path

## Task Commits

Each task was committed atomically:

1. **Task 1: AUDIO-03 — LETTER_WORDS pronunciation fix, wired end-to-end** - `89e085d` (fix)
2. **Task 2: DOCS-01 — branding correction in the two locked doc files** - `7852658` (docs)

**Plan metadata:** pending (this commit)

## Files Created/Modified
- `src/audio.ts` - Added LETTER_WORDS lookup table; widened speakTarget()'s spoken-character resolution chain; corrected DIGIT_WORDS doc comment
- `.claude/CLAUDE.md` - Corrected product name header and deploy-workflow example URL/base-path
- `.planning/PROJECT.md` - Corrected product name header

## Decisions Made
- LETTER_WORDS values follow a phonetic spelled-word convention (Ay, Bee, See, Dee, Ee, Eff, Jee, Aitch, Eye, Jay, Kay, El, Em, En, Oh, Pee, Cue, Ar, Ess, Tee, You, Vee, Double-you, Ex, Why, Zee) — matches DIGIT_WORDS' style of plain lowercase target words for `SpeechSynthesisUtterance`, no per-letter voice tuning needed since the Speech API handles phonetic English words natively
- Left `src/settings-store.ts`'s `STORAGE_KEY` unchanged — explicitly out of scope per the plan's DOCS-01 prohibition (renaming would drop every existing player's persisted settings)

## Deviations from Plan

None - plan executed exactly as written.

## Issues Encountered
None.

## User Setup Required

None - no external service configuration required.

## Known Stubs

None. No hardcoded empty values, placeholder text, or unwired data sources were introduced by either task.

## Threat Flags

None. Both tasks were static edits (a compile-time lookup table, two doc-text replacements) with no new input surface, network call, or persisted-data change, consistent with the plan's threat model (T-03.1-03, T-03.1-04, both `accept`).

## Next Phase Readiness
- AUDIO-03 and DOCS-01 both complete; `npm run build` green after each task
- A human listening pass (per the plan's `<human-check>`) is still recommended to confirm the actual spoken pronunciation across letters on the deployed build — automated build checks cannot assert SpeechSynthesis audio output
- No blockers for Phase 4 / 4.1 / 4.2

---
*Phase: 03.1-bugfix-ux-polish*
*Completed: 2026-08-14*
