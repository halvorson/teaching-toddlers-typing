---
phase: 02.1-progression-trail-celebration-polish
plan: 02
subsystem: ui
tags: [canvas-confetti, celebration, viewport-scaling, typescript]

# Dependency graph
requires:
  - phase: 01-playable-core-loop-live-deploy
    provides: celebrate.ts with fireBurst()/celebrate()/celebrateAlphabetComplete() and the reduced-motion guard
provides:
  - "viewportScaleFactor(): a clamped [0.8, 2.5] viewport-scale factor multiplying confetti startVelocity"
affects: [02.1-03-share-affordance-and-remaining-polish]

actuals:
  tokens: 930
  tasks: 1
  commits: 1

tech-stack:
  added: []
  patterns:
    - "Single scaling site inside fireBurst(): all confetti-tuning concerns (reduced-motion guard, dynamic import, viewport scaling) live in one shared function so every caller inherits them automatically"

key-files:
  created: []
  modified:
    - src/celebrate.ts

key-decisions:
  - "Computed viewportScaleFactor() fresh on every call (no caching, no resize listener) per plan spec — fullscreen transitions are picked up at fire-time with zero lifecycle state"
  - "Only startVelocity is scaled; particleCount/spread/ticks/gravity/scalar stay at each caller's existing literal values so the burst reads as bigger, not faster"

patterns-established:
  - "Viewport-scale tuning co-located with its single consumer (module-private function, not a new module) when it has exactly one call site"

requirements-completed: [CELEB-01, CELEB-02]

coverage:
  - id: D1
    description: "Confetti burst launch velocity scales with viewport size via a clamped geometric-mean factor, applied identically to both celebrate() and celebrateAlphabetComplete()"
    requirement: "CELEB-01"
    verification:
      - kind: unit
        ref: "npm run build (tsc type-check + vite build) — exit 0"
        status: pass
      - kind: other
        ref: "grep assertions confirming viewportScaleFactor() body, single startVelocity multiplication site, and all ten base tuning literals unchanged"
        status: pass
    human_judgment: true
    rationale: "Relative burst size, travel radius, and 'reads as bigger not faster' at real display sizes are perceptual judgements the plan itself designates as human-check only (no static/type check can assert them)."
  - id: D2
    description: "Burst still resolves over the same number of animation frames at the same perceived speed (ticks/gravity untouched) across viewport sizes, and reduced-motion behavior is unchanged"
    requirement: "CELEB-02"
    verification:
      - kind: other
        ref: "grep assertions: ticks/gravity/scalar literals byte-identical to pre-phase values; reduced-motion early-return line still precedes the dynamic import"
        status: pass
    human_judgment: true
    rationale: "Whether the burst 'reads as bigger, never as sped up' is a perceptual judgement across real display sizes, matching the plan's own human-check verification step."

duration: 6min
completed: 2026-08-13
status: complete
---

# Phase 02.1 Plan 02: Viewport-Scaled Celebration Burst Summary

**Confetti burst launch velocity now scales with viewport size via a single clamped geometric-mean factor threaded through `fireBurst()`, the module's one confetti call site.**

## Performance

- **Duration:** 6 min
- **Started:** 2026-08-14T03:04:36Z
- **Completed:** 2026-08-14T03:10:42Z
- **Tasks:** 1
- **Files modified:** 1

## Accomplishments
- Added `viewportScaleFactor()` to `src/celebrate.ts`: geometric mean of `window.innerWidth/1280` and `window.innerHeight/800`, clamped to `[0.8, 2.5]`
- `fireBurst()`'s single `confetti(...)` call now multiplies `opts.startVelocity` by `viewportScaleFactor()`, so both `celebrate()` (ordinary per-match burst) and `celebrateAlphabetComplete()` (Z-completion burst) scale together
- All other tuning literals — `particleCount`, `spread`, `ticks`, `gravity`, `scalar` — and the `prefers-reduced-motion` guard remain byte-identical to Phase 1

## Task Commits

Each task was committed atomically:

1. **Task 1: Scale burst travel distance by a clamped viewport factor inside fireBurst()** - `4b698d8` (feat)

## Files Created/Modified
- `src/celebrate.ts` - Added `viewportScaleFactor()` and the single scaled `startVelocity` multiplication inside `fireBurst()`; updated module head comment to document the new viewport-scaling concern

## Decisions Made
- Followed the plan's explicit formula and clamp bounds exactly (no interpretation needed — the UI-SPEC's "Celebration Scaling Contract" section specified the literal code)
- Left `viewportScaleFactor()` uncached with no resize listener, matching the plan's rationale that a fullscreen transition should be picked up at the next fire-time with zero lifecycle state to manage

## Deviations from Plan

None - plan executed exactly as written.

## Issues Encountered

None. The worktree this plan ran in had branched before Phase 02.1 Plan 01 (star trail) merged to `main`; fast-forwarded to `main` first to pick up the missing planning docs (`02.1-02-PLAN.md`, `02.1-RESEARCH.md`, `02.1-PATTERNS.md`) and Plan 01's base files before starting Task 1. No conflicts — the fast-forward was a pure ancestor merge with zero divergent commits on this branch.

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness

`src/celebrate.ts` is the only file this plan touched; `viewportScaleFactor` is not referenced anywhere else in `src/` (confirmed via `grep -rl`). Ready for Plan 03 (share affordance and remaining polish), which does not depend on this plan's output. The human-check verification (resizing the browser / going fullscreen and comparing burst size at small vs. large viewports, then playing Alphabet mode to Z) is still outstanding and deferred to end-of-phase human verification per `human_verify_mode: end-of-phase` in `.planning/config.json`.

---
*Phase: 02.1-progression-trail-celebration-polish*
*Completed: 2026-08-13*

## Self-Check: PASSED

- FOUND: src/celebrate.ts
- FOUND: .planning/phases/02.1-progression-trail-celebration-polish/02.1-02-SUMMARY.md
- FOUND commit: 4b698d8 (feat(02.1-02): scale celebration burst velocity by clamped viewport factor)
- FOUND commit: 9e9f0f0 (docs(02.1-02): complete viewport-scaled celebration burst plan)
