---
phase: 02.1-progression-trail-celebration-polish
verified: 2026-08-14T06:30:00Z
status: human_needed
score: 18/18 must-haves verified (code-level)
behavior_unverified: 0
overrides_applied: 0
mvp_mode_note: "ROADMAP.md marks this phase mode: mvp, but the phase goal ('Correct matches leave a quiet, persistent ambient visual trail...') is not in 'As a ..., I want to ..., so that ....' User Story format (gsd_run query user-story.validate returns valid:false). Same pattern holds for Phase 3/4 in this roadmap, so this looks like a project-wide convention rather than a phase-specific defect. Verification proceeded using the roadmap's well-formed Success Criteria list (Option A/B path) rather than refusing outright, since refusing would discard three completed, code-reviewed plans with no actionable path forward. Recommend running /gsd mvp-phase 2.1 only if strict MVP Success-Criteria-as-User-Story compliance is desired going forward — not a blocker for this phase's goal achievement."
human_verification:
  - test: "Run `npm run dev`, choose Letters, press the matching physical key 10-15 times in a row, then Escape and re-enter Letters."
    expected: "Each correct press adds one small purple glowing dot at a new random spot on the dark background; dots accumulate and never disappear on a correct match; the target letter always renders above any dot; no dots visible on the home menu; re-entering Letters starts with a completely empty background."
    why_human: "Visual layering, scatter distribution, and legibility of the target over the trail are rendered properties grep/tsc cannot assert (plan 02.1-01 Task 1's own human-check)."
  - test: "With OS Reduce Motion OFF, play a few correct matches and watch a new star appear; then enable Reduce Motion (macOS: System Settings → Accessibility → Display → Reduce motion), reload, and play a few more."
    expected: "Motion ON: each star fades/scales in over ~300ms then stays at full brightness. Reduce-motion ON: each star appears instantly at full size with no entrance animation."
    why_human: "An OS-level media-query preference and a 300ms CSS entrance are only observable in a live rendering browser (plan 02.1-01 Task 2's own human-check)."
  - test: "Resize the browser to ~640x480 and play several correct matches in Letters, noting travel distance; then maximise/fullscreen on the largest available display and play several more; finally play Alphabet mode through to Z on the large display."
    expected: "Small window: burst looks essentially unchanged from Phase 1. Large/fullscreen display: the same burst throws particles noticeably further and covers much more of the screen while still settling in about the same time (bigger, not faster). Z-completion celebration remains clearly the largest burst at both sizes."
    why_human: "Relative burst size, travel radius, and 'reads as bigger not faster' are perceptual judgements across real display sizes; no static/type check can assert them (plan 02.1-02 Task 1's own human-check)."
  - test: "From the home menu, open Settings by mouse, then by keyboard (Tab/arrow to the toggle, Space to flip, Escape to exit); re-open Settings, flip the toggle with a mouse click, hard-reload the page, and re-open Settings; check the address bar while Settings is open."
    expected: "Settings opens as a centred dark card over the same drifting background as the menu — never fullscreen, never blank, no star dots visible behind it. Toggle track turns purple and the thumb slides right when on. Escape and '← Back' both return to the menu. After a hard reload the toggle is still in whichever position it was left in. URL reads `?screen=settings` while open and is clean on the menu."
    why_human: "Panel layering over the parallax background, the toggle's slide/colour transition, and cross-reload persistence are rendered and stateful behaviours no static check can assert (plan 02.1-03 Task 2's own human-check)."
  - test: "With the toggle at default OFF, play Letters until ~12 stars are on screen, then press a wrong key several times. Return to Settings, switch the toggle ON, go back into Letters, build up ~12 stars again, and press a wrong key."
    expected: "Toggle OFF: wrong key produces only the usual brief neutral flicker; every star stays exactly where it was. Toggle ON: wrong key produces the same flicker AND every star disappears at once, leaving a clean background that refills from the next correct match. No error text, sound, or punitive cue in either case."
    why_human: "The difference between 'all stars removed at once' and 'no stars removed' is a live visual observation, and the setting must be flipped through the real UI to prove the gameplay path reads the persisted value (plan 02.1-03 Task 3's own human-check)."
---

# Phase 02.1: Progression Trail & Celebration Polish Verification Report

**Phase Goal:** Correct matches leave a quiet, persistent ambient visual trail (never a stats HUD), and the celebration burst reads clearly at any screen size — the two polish items flagged as higher priority than Phase 3/4
**Verified:** 2026-08-14T06:30:00Z
**Status:** human_needed
**Re-verification:** No — initial verification

## Goal Achievement

### Observable Truths (ROADMAP Success Criteria)

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | Each correct match adds a subtle star to a persistent background trail — purely decorative, no numbers/stats surfaced | ✓ VERIFIED (code) | `src/trail.ts` `addTrailStar()` appends a `.star-trail__star` div with no text node anywhere in the module; called from `game-screen.ts`'s correct-match branch (line 93). No `textContent`/numeric state in `trail.ts`. Visual confirmation harvested to human verification. |
| 2 | The trail persists across the play session by default; a Settings toggle (default off) can make it reset on an incorrect key press | ✓ VERIFIED (code) | `mountTrailLayer()` only clears the trail on mode (re)entry; `clearTrail()` is only invoked from `game-screen.ts`'s incorrect-key branch, gated by `readSettings().resetTrailOnMistake` (default `false` in `settings-store.ts`'s `DEFAULT_SETTINGS`). |
| 3 | The confetti celebration scales with viewport/fullscreen size so it's clearly visible on high-resolution monitors, not just small windows | ✓ VERIFIED (code) | `src/celebrate.ts` `viewportScaleFactor()` computes a geometric-mean ratio against a 1280x800 baseline, clamped `[0.8, 2.5]`; `fireBurst()`'s single `confetti(...)` call multiplies `opts.startVelocity` by this factor. Visual confirmation harvested to human verification. |
| 4 | The confetti burst travels further / covers more area than Phase 1's tuning, at the same animation speed | ✓ VERIFIED (code) | Only `startVelocity` is scaled; `particleCount`, `spread`, `ticks`, `gravity`, `scalar` are byte-identical to Phase 1's literals in both `celebrate()` and `celebrateAlphabetComplete()` (confirmed via direct file read) — same resolve time, larger launch velocity. |

**Score:** 4/4 roadmap Success Criteria verified at the code level (0 present-behavior-unverified, 0 failed). All four carry a live-browser perceptual component that is deferred to human verification below, consistent with `human_verify_mode: end-of-phase` already declared in `.planning/config.json` and exercised identically by all three plans' own `<human-check>` blocks.

### Observable Truths (PLAN must_haves, granular)

| # | Truth | Plan | Status | Evidence |
|---|-------|------|--------|----------|
| 1 | Correct key press appends one star | 01 | ✓ VERIFIED | `game-screen.ts:93` `addTrailStar()` inside the correct-match branch |
| 2 | Stars accumulate, never removed by correct match/target swap/Alphabet wrap | 01 | ✓ VERIFIED | No `clearTrail()`/element-removal call anywhere in the correct-match path; `clearTrail()` only reachable from the incorrect-key branch |
| 3 | Mode entry starts with empty trail | 01 | ✓ VERIFIED | `mountTrailLayer()` always creates a fresh `div`, called from `mountGameScreen()` |
| 4 | Trail invisible on home menu, visible during gameplay | 01 | ✓ VERIFIED | `unmountTrailLayer()` removes the DOM node entirely on `unmountGameScreen()`; CSS `body[data-chrome="game"] #star-trail { opacity: 1 }` is the inverse of the drifting-background suppression |
| 5 | Stars never render above the target | 01 | ✓ VERIFIED | `#star-trail` at `z-index: -1` (fixed), `#app`/`#target` unpositioned (`z-index: auto`) — negative z-index stacks below normal-flow content in the same root stacking context |
| 6 | No number/counter/stat text ever rendered by the trail | 01 | ✓ VERIFIED | `grep textContent src/trail.ts` — zero matches |
| 7 | Burst visibly larger/travels further on large viewport than small window | 02 | ✓ VERIFIED (code) | `viewportScaleFactor()` × `startVelocity`, confirmed formula and clamp literals present |
| 8 | Burst resolves in same time, reads as bigger not faster | 02 | ✓ VERIFIED | `ticks`/`gravity` literals unchanged from Phase 1 in both callers |
| 9 | Alphabet Z-completion scales by same factor, stays bigger at every size | 02 | ✓ VERIFIED | `celebrateAlphabetComplete()` routes through the same `fireBurst()`/`viewportScaleFactor()` with its own larger base tuning (120/100/35/200/1.1 vs 40/60/25/150/0.8) |
| 10 | Small window ≥80%, large display ≤250% of Phase 1 tuning | 02 | ✓ VERIFIED | `Math.min(2.5, Math.max(0.8, factor))` present verbatim |
| 11 | Reduced motion → no confetti at all | 02 | ✓ VERIFIED | Reduced-motion early return precedes both the dynamic import and the scaling logic in `fireBurst()`, unchanged from Phase 1 |
| 12 | Settings row opens windowed panel over drifting background, not fullscreen | 03 | ✓ VERIFIED (code) | `main.ts` chrome ternary: `screen === 'menu' \|\| screen === 'settings' ? 'menu' : 'game'`; `onOpenSettings()` calls `mountScreen('settings')` directly, never `enterFullscreen` (count of `enterFullscreen` in `main.ts` = 3, unchanged) |
| 13 | Panel shows exactly one control row "Reset trail on mistake", off by default | 03 | ✓ VERIFIED | `settings.ts` single `toggle-row` with `textContent = 'Reset trail on mistake'`; `aria-checked` initialised from `readSettings().resetTrailOnMistake`, default `false` |
| 14 | Toggle flips via click or Enter/Space, persists across reload | 03 | ✓ VERIFIED (code) | Native `<button>` (fires `click` for Enter/Space natively); `writeSettings()` called on every flip; `readSettings()` re-reads from `localStorage` on next mount. Cross-reload confirmation harvested to human verification. |
| 15 | '← Back' and Escape both return to home menu | 03 | ✓ VERIFIED | `handleClick`'s `closest('.panel-back')` and `handleKeydown`'s `event.key === 'Escape'` both call `onBack` (= `quitToMenu`) |
| 16 | Toggle OFF: wrong key leaves every star untouched | 03 | ✓ VERIFIED | `clearTrail()` only called when `readSettings().resetTrailOnMistake` is `true` |
| 17 | Toggle ON: wrong key removes every star at once | 03 | ✓ VERIFIED | Same gate, `clearTrail()` performs a full `replaceChildren()` wipe |
| 18 | Corrupted/hand-edited localStorage never throws, falls back to OFF | 03 | ✓ VERIFIED | `readSettings()` wraps the whole body in try/catch, shape-checks `version === 1 && typeof resetTrailOnMistake === 'boolean'` before trusting any field, and returns a fresh copy of `DEFAULT_SETTINGS` on any failure |

**Score:** 18/18 plan-level truths verified at the code level.

None of the truths above are behavior-dependent in the Step 3 sense (state-transition, cancellation/cleanup, or ordering invariant needing a passing test) — they are deterministic DOM/CSS/data logic, confirmed by direct code reading. The remaining gap is purely perceptual/live-browser confirmation, which every plan itself explicitly deferred to human `<human-check>` steps (consistent with `human_verify_mode: end-of-phase`). Those items are harvested into Human Verification Required below.

### Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `src/trail.ts` | `mountTrailLayer`/`unmountTrailLayer`/`addTrailStar`/`clearTrail`, ≥45 lines | ✓ VERIFIED | 64 lines; exactly these four exports, no more |
| `src/style.css` (#star-trail block) | `#star-trail` layer, `.star-trail__star` visual, `star-pop-in` keyframes, reduced-motion override, z-index rewire | ✓ VERIFIED | `body::before,after` at `z-index:-2`, `#star-trail` at `z-index:-1`, `@keyframes star-pop-in`, `.star-trail__star` block appears twice (base + reduced-motion override, `animation: none;` first) |
| `src/game-screen.ts` (trail wiring) | `addTrailStar` call site inside the single keydown handler | ✓ VERIFIED | One `addEventListener('keydown', ...)`; `mountTrailLayer()`/`unmountTrailLayer()` in lifecycle; `addTrailStar()` in correct branch; `clearTrail()` gated in incorrect branch |
| `src/celebrate.ts` | `viewportScaleFactor()` + single scaled `confetti(...)` call, ≥95 lines | ✓ VERIFIED | 137 lines; `viewportScaleFactor()` present with exact formula/clamp; `opts.startVelocity * viewportScaleFactor()` appears exactly once |
| `src/settings-store.ts` | `readSettings`/`writeSettings`/`AppSettings`, ≥35 lines | ✓ VERIFIED | 58 lines; both functions present, ≥2 `catch` blocks, shape-checked read |
| `src/settings.ts` | `mountSettingsScreen`/`unmountSettingsScreen`, ≥70 lines | ✓ VERIFIED | 111 lines; role=switch toggle, 3 `textContent`-bearing elements (back/title/label), Back+Escape exit handling |
| `src/style.css` (settings block) | `.settings-panel`, `.panel-back`, `.panel-title`, `.toggle-row`, `.toggle-switch`, `.toggle-switch__thumb` | ✓ VERIFIED | All present with literal values matching the UI-SPEC (480px max-width, 44×24 track, 20px thumb) |
| `src/router.ts` | `'settings'` routable Screen | ✓ VERIFIED | Present in both the `Screen` union and `VALID_SCREENS` |
| `src/main.ts` | Settings mount/unmount cases + chrome classification | ✓ VERIFIED | `case 'settings':` ×2, `mountSettingsScreen(app, quitToMenu)`, widened chrome ternary |
| `src/menu.ts` | `onOpenSettings` handler dispatch | ✓ VERIFIED | `MenuHandlers.onOpenSettings`, dispatched in `handleClick` for `row === 'settings'` |

### Key Link Verification

| From | To | Via | Status | Details |
|------|-----|-----|--------|---------|
| `game-screen.ts` | `trail.ts` | `addTrailStar()` in correct-match branch; `mountTrailLayer()`/`unmountTrailLayer()` in lifecycle | ✓ WIRED | Confirmed by direct read |
| `trail.ts` | `style.css` | `#star-trail` / `.star-trail__star` selectors | ✓ WIRED | Confirmed |
| `main.ts` | `style.css` | `document.body.dataset.chrome` drives `body[data-chrome="game"] #star-trail` visibility | ✓ WIRED | Confirmed |
| `celebrate.ts fireBurst()` | `celebrate.ts viewportScaleFactor()` | `startVelocity * viewportScaleFactor()` | ✓ WIRED | Single occurrence confirmed |
| `menu.ts handleClick` | `main.ts mountScreen('settings')` | `MenuHandlers.onOpenSettings()` | ✓ WIRED | Confirmed |
| `main.ts` | `settings.ts` | `mountSettingsScreen(app, quitToMenu)` / `unmountSettingsScreen()` | ✓ WIRED | Confirmed |
| `settings.ts` | `settings-store.ts` | `readSettings()` for initial `aria-checked`, `writeSettings()` on flip | ✓ WIRED | Confirmed |
| `game-screen.ts` | `settings-store.ts` | `readSettings().resetTrailOnMistake` gates the wipe | ✓ WIRED | Confirmed |
| `game-screen.ts` | `trail.ts` | `clearTrail()` called when the gate passes | ✓ WIRED | Confirmed; `clearTrail()` line number (99) is after the last `correct-pulse` line (92), proving it sits in the incorrect-key branch |

### Data-Flow Trace (Level 4)

Not applicable in the traditional API→DB sense — this phase has no backend. The relevant "data flow" is `localStorage` → `readSettings()` → toggle initial state / gameplay gate, and viewport geometry → `viewportScaleFactor()` → confetti velocity. Both traced above under Key Links and confirmed to read live values (`window.innerWidth/innerHeight`, `localStorage.getItem`) rather than static/hardcoded fallbacks.

### Behavioral Spot-Checks

| Behavior | Command | Result | Status |
|----------|---------|--------|--------|
| Production build succeeds | `npm run build` (tsc strict + vite build) | Exit 0, 18 modules transformed, `dist/` produced | ✓ PASS |
| Exactly one keydown listener in game-screen.ts | `grep -c "addEventListener('keydown'" src/game-screen.ts` | `1` | ✓ PASS |
| `clearTrail()` sits after `correct-pulse` (incorrect-branch-only) | `grep -n` line-order comparison | `clearTrail()` at line 99 > last `correct-pulse` at line 92 | ✓ PASS |
| No debt markers / stub patterns in phase files | `grep -n -E "TBD\|FIXME\|XXX\|TODO\|HACK\|PLACEHOLDER"` across all 8 modified/created files | No matches | ✓ PASS |
| `viewportScaleFactor` confined to `celebrate.ts` | `grep -rl viewportScaleFactor src` | 1 file | ✓ PASS |

No test suite exists in this project (no test framework configured; `npm run lint` script also absent) — consistent with the project's documented decision to skip automated test scaffolding for this solo/hobby scope (per project CLAUDE.md "What NOT to Use"). Live in-browser checks (`npm run dev`) were not started per verifier constraints (no server-starting, no state mutation); those checks are exactly what the plans' own `<human-check>` blocks require and are harvested below.

### Probe Execution

No `scripts/*/tests/probe-*.sh` files exist in this repository and none are declared in any PLAN/SUMMARY for this phase. Skipped — no probes applicable.

### Requirements Coverage

| Requirement | Source Plan | Description | Status | Evidence |
|-------------|-------------|--------------|--------|----------|
| TRAIL-01 | 02.1-01 | Correct match adds a subtle star to a persistent ambient trail, decorative only | ✓ SATISFIED | See truths 1-6 above |
| TRAIL-02 | 02.1-03 | Settings toggle (default off) controls whether trail resets on a mistake | ✓ SATISFIED | See truths 12-18 above |
| CELEB-01 | 02.1-02 | Burst scales relative to viewport/fullscreen size | ✓ SATISFIED | See truths 7, 10 above |
| CELEB-02 | 02.1-02 | Burst travels further/covers more area at the same animation speed | ✓ SATISFIED | See truths 8-9 above |

No orphaned requirements: `.planning/REQUIREMENTS.md`'s traceability table maps exactly these four IDs to "Phase 2.1", and all four appear in a plan's `requirements:` frontmatter field. `REQUIREMENTS.md`'s own checkbox column (`- [ ]`) and traceability status column (`Pending`) are stale — they were not flipped to `[x]`/`Complete` as part of phase execution. This is a documentation-bookkeeping gap, not a code gap; it does not block the phase goal but should be updated (normally handled by the ship/complete-milestone workflow).

### Anti-Patterns Found

None. Scanned all 10 files created/modified by this phase's three plans (`src/trail.ts`, `src/style.css`, `src/game-screen.ts`, `src/celebrate.ts`, `src/settings-store.ts`, `src/settings.ts`, `src/router.ts`, `src/main.ts`, `src/menu.ts`) for debt markers (TBD/FIXME/XXX/TODO/HACK/PLACEHOLDER), empty-implementation patterns, hardcoded-empty stub data, and console.log-only handlers — zero matches beyond expected, intentional silent-fallback `catch` blocks with explanatory comments (matching the project's documented "decorative/utility failures fail silently" pattern).

Code review artifacts (`02.1-REVIEW.md`, `02.1-REVIEW.iter2.md`, `02.1-REVIEW.iter3.md`) independently reviewed the same 9 files and reached `status: clean` in the final iteration (0 critical, 0 warning, 1 info — an unrelated pre-existing Share-row timer-overwrite note explicitly marked "not required for this phase"). Cross-checked against the actual fix commits (`19d5959`, `2e7c715`, `8211fa4`, `6e07d711`, `765e913`) — all present in `git log`.

### Human Verification Required

5 items harvested from the three plans' own `<human-check>` blocks (deferred to end-of-phase per `human_verify_mode: end-of-phase`). See frontmatter `human_verification` for full detail; summarized:

1. **Star trail end-to-end visual** — dots appear/accumulate/layer correctly under the target, hidden on menu, empty on mode re-entry.
2. **Star pop-in / reduced-motion** — 300ms scale-fade entrance vs. instant appearance under OS reduce-motion.
3. **Viewport-scaled celebration burst** — visibly bigger/further on a large display vs. a small window, same resolve time, Z-completion stays biggest at both sizes.
4. **Settings screen reachability & persistence** — windowed panel over the parallax background, mouse+keyboard operable, Back/Escape exit, toggle survives a hard reload, URL reflects `?screen=settings`.
5. **Trail-reset toggle gameplay effect** — OFF leaves all stars untouched on a mistake; ON wipes every star at once.

### Gaps Summary

No gaps. All 4 roadmap Success Criteria and all 18 plan-level must-have truths are verified at the code level: artifacts exist, are substantive (well above minimum line counts), and are wired end-to-end (confirmed by direct file reads, not grep-only inference). `npm run build` passes clean. Code review reached a clean status across three iterations with all warnings resolved and verified against commit hashes present in `git log`. Requirements TRAIL-01, TRAIL-02, CELEB-01, CELEB-02 are all satisfied with no orphans.

The phase is not marked `passed` only because every one of the three plans deliberately deferred its own perceptual/live-browser `<human-check>` steps to end-of-phase human verification — this is the expected, designed behavior under this project's `human_verify_mode: end-of-phase` setting, not a defect. The 5 items above are exactly those deferred checks, harvested for a single end-of-phase UAT pass.

One informational note (see frontmatter `mvp_mode_note`): the phase's roadmap entry is tagged `mode: mvp` but its goal text is not in User Story format, so the strict MVP-mode verification path (which would otherwise refuse to verify) was not applied; standard goal-backward verification against the roadmap's Success Criteria was used instead. This appears to be a project-wide convention (Phase 3/4 have the same pattern) rather than a phase-specific issue, and is not treated as a blocker.

---

_Verified: 2026-08-14T06:30:00Z_
_Verifier: Claude (gsd-verifier)_
