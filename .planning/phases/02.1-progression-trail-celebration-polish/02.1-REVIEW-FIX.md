---
phase: 02.1-progression-trail-celebration-polish
fixed_at: 2026-08-14T05:54:25Z
review_path: .planning/phases/02.1-progression-trail-celebration-polish/02.1-REVIEW.md
iteration: 2
findings_in_scope: 1
fixed: 1
skipped: 0
status: all_fixed
---

# Phase 02.1: Code Review Fix Report

**Fixed at:** 2026-08-14T05:54:25Z
**Source review:** .planning/phases/02.1-progression-trail-celebration-polish/02.1-REVIEW.md
**Iteration:** 2

**Summary:**
- Findings in scope: 1
- Fixed: 1
- Skipped: 0

## Fixed Issues

### WR-02-RESIDUAL: `.focused` class resync only survives the manual-copy textbox's first blur

**Files modified:** `src/menu.ts`
**Commit:** 765e913
**Applied fix:** Read the full lifecycle of `shareFallbackEl`/`urlBox` in `renderManualFallback()`, `resetShareFeedback()`, and `unmountMenu()` before choosing a fix shape, and confirmed the review's stated rationale holds: `urlBox` (and any listener attached to it) is discarded every time `resetShareFeedback()` runs or the menu unmounts, so a persistent (non-one-shot) listener carries no leak risk. Removed `{ once: true }` from the `blur` listener registered in `renderManualFallback()` at `src/menu.ts:175` (now `src/menu.ts:179`), so the `.focused` class resync (`syncFocusedClass()`) fires on every blur of the manual-copy textbox — not just the first — for as long as the fallback panel remains mounted. This closes the reproduction path where a second Tab-in/Tab-out cycle on `urlBox` (while `handleFocusIn` continues to ignore the non-`button[data-row]` textbox) left the `.focused` indicator stale. Updated the surrounding comment block to document why the listener is deliberately not one-shot. Verified with `node -c src/menu.ts` (syntax check passed) and a re-read of the modified section confirming the fix text is present and surrounding code (the explicit `.focused`-clear on entry, the `urlBox.select()` call, and the rest of `renderManualFallback()`) is intact.

---

_Fixed: 2026-08-14T05:54:25Z_
_Fixer: Claude (gsd-code-fixer)_
_Iteration: 2_
